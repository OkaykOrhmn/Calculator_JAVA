<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CalculatorJAVA" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/backButtonsDark</item>
        <item name="colorPrimaryVariant">@color/buttonsDark</item>
        <item name="colorOnPrimary">@color/backDark</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/back</item>

        <item name="android:textColor">@color/back</item>
        <item name="android:background">@color/backDark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/backDark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>